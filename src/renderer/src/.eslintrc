{
  "extends": ["react-app"],
  "rules": {
    "no-undef": "off"  // Disables the rule entirely—safe if you're not worried about undef vars
  },
  "parserOptions": {
    "ecmaVersion": "latest"  // Ensures modern JS features like Iterator are recognized
  },
  "env": {
    "es2022": true,  // Adds support for ES2022 globals including Iterator
    "browser": true
  }
}